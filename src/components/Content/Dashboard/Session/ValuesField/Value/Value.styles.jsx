import styled from "styled-components"
export const ValueContainer = styled.div`
    display: flex;
    align-items: center;
    gap: 4px;

    svg {
        fill: var(--white);
    }
`

export const TinyTim = styled.span`
    font-size: var(--fontSuperTiny);
`
